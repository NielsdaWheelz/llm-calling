# Live Evidence

Evidence qualifies only the code revision and external boundary that produced
it. Codex records dated through 2026-09-08 predate the shared WebSocket/Unix
socket boundary and remain historical records; they do not qualify the current
runtime. Run the current opt-in live gates to produce current Codex evidence.

The 2026-09-10 Codex records observe source commit
`69d41d38a3d290e7ae3bde9b57556dda41e1b2f1` against native 0.154.0:

- The standard Codex matrix uses Terra with catalog-default reasoning and records
  same-thread accounting, resume, and real commentary/final selection. Its normal
  policy recorded `AgentToolUse`; it is not the contained-cognition proof.
- The separate Terra/high containment case records no native authority and no
  host effect. It does not claim an attempted native execution was blocked.
- The automatic-compaction fixture uses Terra/high and a per-thread threshold of
  1024, with three successful turns, two completed compactions, two synthetic usage
  estimates, and a full client reopen. It does not qualify the production threshold.

These are Codex-only observations, not the full registry and Codex-and-Claude
release matrix. The evidence-only followup leaves the qualified runtime source
unchanged. Historical records and earlier failed qualification attempts are not
superseded by these new observations.

## One-release maintenance exception — 2026-09-10

The owner explicitly approved Codex-only qualification for this native-compaction
maintenance release and its exact Jarvis dependency propagation. The exception
is bound to runtime commit `69d41d38a3d290e7ae3bde9b57556dda41e1b2f1`; later
evidence/documentation-only commits do not change that qualified code.

The accepted scope is exactly the observations above and these artifacts:

- [Standard Codex matrix](agent-runtime-codex-sdk-local_account-2026-09-10-32ae5545775c.json).
- [Separate containment](codex-native-containment-2026-09-10-5720d896fc8c.json).
- [Synthetic-threshold automatic compaction](codex-native-compaction-2026-09-10-ba1a15d2b5b2.json).

The full paid registry and Claude backend remain NOT_RUN and are not qualified
by this release. Approval accepts that missing live coverage for this release;
it does not certify unchanged API or Claude behavior. The
[general release policy](../../../docs/pivot-spec.md#11-testing)
and future release/Nexus qualification requirements remain unchanged. This
exception expires with this maintenance release and authorizes no later source
change. Approval and evidence alone do not assert a completed merge or deployment.

## Version observations

Current evidence does not synthesize a native version from a package constant.
Record actual installed CLI and running-server observations separately during
qualification; they may differ after an install that leaves a healthy service
running. Neither observation is runtime admission authority.
